// Copyright 2018 Your Name <your_email>

#include <iostream>
#include <tasks.hpp>
#include <vector>

using namespace std;

// Task 3.
void swap_el(std::vector<int>& A) {
  swap(A[0], A[17]);
  swap(A[1], A[18]);
  swap(A[2], A[19]);
}
